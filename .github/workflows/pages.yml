# Disabled: repo uses Deploy-from-branch Pages, not Actions.
# Keeping this file empty of push triggers avoids red X on every commit.
name: Deploy GitHub Pages (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages is deployed from branch main/(root); this Actions workflow is intentionally disabled."
